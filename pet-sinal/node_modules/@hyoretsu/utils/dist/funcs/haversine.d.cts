/**
 * Applies the haversine function to a degree.
 *
 * @param degree    -   Degree in radians.
 */
declare const haversine: (degree: number) => number;
/**
 * Applies the archaversine function to a degree.
 *
 * @param degree    -   Degree in radians.
 */
declare const archaversine: (degree: number) => number;

export { archaversine, haversine };

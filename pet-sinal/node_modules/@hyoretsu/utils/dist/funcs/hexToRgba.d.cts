type RGB = [number, number, number];
type RGBA = [number, number, number, number];
type ColorArray = RGB | RGBA;
declare const hexToRgba: (hex: string) => ColorArray;

export { ColorArray, RGB, RGBA, hexToRgba };
